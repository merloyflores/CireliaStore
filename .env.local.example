# Copiar a .env.local y completar.
# La URL y la anon key son públicas por diseño (se usan en el navegador);
# la seguridad real la da RLS en la base de datos, no mantener esto en secreto.

NEXT_PUBLIC_SUPABASE_URL=https://skwlxwliyfpljkhtxzar.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Slug del tenant por defecto cuando no hay subdominio real (local/preview).
# Ver middleware.ts -> resolveTenantSlug(). Hoy solo existe "cirelia".
NEXT_PUBLIC_DEFAULT_TENANT_SLUG=cirelia
